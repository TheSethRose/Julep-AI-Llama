# Julep Configuration
# Required: Your Julep API key from https://dev.julep.ai
JULEP_API_KEY=your_julep_api_key_here

# LLM Configuration
# Optional: Custom OpenAI-compatible API endpoint (defaults to https://api.openai.com)
OPENAI_API_BASE=https://api.openai.com
# Optional: Not required for Llama endpoint
OPENAI_API_KEY=not-needed

# Storage Configuration
# Optional: SQLite database path (defaults to ./storage/julep.db)
SQLITE_DB_PATH=/workspaces/julep/storage/julep.db

# ChromaDB Configuration
# Optional: ChromaDB storage directory (defaults to ./storage/chromadb)
CHROMA_DB_DIR=/workspaces/julep/storage/chromadb

# Redis Configuration
# Optional: Redis connection settings (defaults shown)
REDIS_HOST=127.0.0.1
REDIS_PORT=6379

# Logging Configuration
# Optional: Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO
# Optional: Log file path (defaults to ./storage/logs/julep.log)
LOG_FILE=/workspaces/julep/storage/logs/julep.log
